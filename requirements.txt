numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
statsmodels>=0.14
tqdm
jupyterlab
notebook
ipython
# lightfm is installed by scripts/install_lightfm.sh — its sdist (1.17) has an
# upstream packaging bug that breaks PEP-517 isolated builds. Conda users get
# a working build straight from conda-forge.
